<!DOCTYPE html>

<html>
<head>
<meta charset = "utf-8">
<title>ABM Web - Todo List</title>
<style>
  body{
    font-family: "Optima";
  }
</style>
</head>

<body>
<table class = "table table-striped table-bordered table-hover">   <!-- table striped not working -->
  <thead class = "table-dark">
    <tr>
      <th>Task</th>
      <th>Status</th>
      <th>Due</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.task %></td>
        <td><%= task.status %></td>
        <td><%= task.due %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path, class:"btn btn-secondary" %>

<!--- log out --->
<%= link_to 'Log Out', destroy_user_session_path, method: :delete, class:"btn btn-warning"%>
</body>

</html>
